opnsense:
  hosts:
    192.168.1.1:
  vars:
    ansible_connection: local
    firewall: "https://{{ inventory_hostname }}"
    api_key: "YOUR_API_KEY"
    api_secret: "YOUR_API_SECRET"
    ssl_verify: false
